@model IEnumerable<CourseEvaluationSystem.Models.Course>

<h2>Courses</h2>

<ul>
    @foreach (var course in Model)
    {
        <li>
            @course.Title |
            <a asp-controller="Evaluation" asp-action="Create" asp-route-courseId="@course.Id">Evaluate</a>
        </li>
    }
</ul>
